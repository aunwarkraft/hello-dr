# Database
DATABASE_URL=sqlite:///./appointments.db

# Application Environment
APP_ENV=development

# Timezone
TIMEZONE=America/Toronto

# CORS Origins (JSON array format, e.g., ["http://localhost:3000"])
CORS_ORIGINS=["http://localhost:3000"]
